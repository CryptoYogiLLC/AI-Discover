apiVersion: 1

providers:
  - name: "AI-Discover Dashboards"
    orgId: 1
    folder: "AI-Discover"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
